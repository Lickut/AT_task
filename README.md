# AT_task - Using Selenium with C#.NET

To run the Selenium tests, download the solution, change  *tablePageURL* field (*TestHireDateOrder.cs* file) to *table_example.html* file location and run the NUnit tests.
